import 'package:flutter/material.dart';

class RoutesView extends StatelessWidget {
  const RoutesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Routes"));
  }
}
